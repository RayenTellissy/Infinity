import React from 'react';

// components
import AvatarSection from './components/AvatarSection';

const Settings = () => {

  return (
    <div>
      <AvatarSection />
    </div>
  );
};

export default Settings;